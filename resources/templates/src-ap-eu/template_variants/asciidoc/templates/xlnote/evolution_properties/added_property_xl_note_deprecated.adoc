
{% import "macros.adoc" as mc %}

    {% set content, error = from_endpoint(conf.default_endpoint).with_query_from_file(conf.query_files["added_property_xl_note_deprecated.rq"]).fetch_tabular() %}
    
{% if not content.empty %}
    {% call mc.render_fetch_results(content, error) %}
    {% set compress_uris = simplify_uri_columns_in_tabular(data_frame=content,namespace_inventory=namespaces,error_fail=False) %}
    
==== Added deprecated
    {{ mc.pandas_table(content, "Added deprecated") }}
    
    {% endcall %}
{% endif %}
